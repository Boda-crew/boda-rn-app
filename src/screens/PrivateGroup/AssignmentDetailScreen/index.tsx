import React from 'react';
import { AScrollView, AText, Container } from '@components';

export const AssignmentDetailScreen = () => {
  return (
    <Container>
      <AScrollView>
        <AText>AssignmentDetailScreen</AText>
      </AScrollView>
    </Container>
  );
};
