import React from 'react';
import { AScrollView, AText, Container } from '@components';

interface Props {}

export const AccountScreen = (props: Props) => {
  return (
    <Container>
      <AScrollView>
        <AText>AccountScreen</AText>
      </AScrollView>
    </Container>
  );
};
