import React from 'react';
import { AScrollView, AText, Container } from '@components';

interface Props {}

export const StudyScreen = (props: Props) => {
  return (
    <Container>
      <AScrollView>
        <AText>StudyScreen</AText>
      </AScrollView>
    </Container>
  );
};
