import React from 'react';
import { AScrollView, AText, Container } from '@components';

interface Props {}

export const NoticeScreen = (props: Props) => {
  return (
    <Container>
      <AScrollView>
        <AText>NoticeScreen</AText>
      </AScrollView>
    </Container>
  );
};
