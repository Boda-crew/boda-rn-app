import React from 'react';
import { AScrollView, AText, Container } from '@components';

interface Props {}

export const NotificationScreen = (props: Props) => {
  return (
    <Container>
      <AScrollView>
        <AText>NotificationScreen</AText>
      </AScrollView>
    </Container>
  );
};
